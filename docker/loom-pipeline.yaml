apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    run: loom-pipeline
  name: loom-pipeline
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      run: loom-pipeline
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        run: loom-pipeline
    spec:
      containers:
      - image: gcr.io/linnarsson-lab/loom-pipeline:v1
        imagePullPolicy: Always
        name: loom-pipeline
        resources: {}
        terminationMessagePath: /dev/termination-log
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      securityContext: {}
      terminationGracePeriodSeconds: 30

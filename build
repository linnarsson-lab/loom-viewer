#! /bin/bash

./build_client
build_status=$?
if [ $build_status == 0 ]; then
  ./build_egg
else
  echo ""
  echo ""
  echo " Minification not finished properly, aborting build"
  echo ""
  exit $build_status
fi